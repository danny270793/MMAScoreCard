import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  "appId": "io.github.danny270793.mmascorecard",
  "appName": "MMA ScoreCard",
  "webDir": "www",
  plugins: {
    SplashScreen: {
      backgroundColor: "#ff0166da",
    }
  }
}

export default config;
