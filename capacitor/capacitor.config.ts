import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  "appId": "io.github.danny270793.mmascorecard",
  "appName": "mmascorecard",
  "webDir": "www"
}

export default config;
